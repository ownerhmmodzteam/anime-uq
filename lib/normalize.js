export function normalizeItem(x = {}, source = '') { return { id: x.id || x.slug || x.url || crypto.randomUUID(), title: x.title || x.name || 'Untitled', slug: x.slug || '', url: x.url || '', cover: x.cover || x.image || x.thumbnail || x.poster || '', description: x.description || x.synopsis || '', type: x.type || 'Anime', status: x.status || '', latestChapter: x.latestChapter || x.chapter || x.episode || '', source, key: String(source||'local')+'::'+String(x.id || x.slug || x.url || x.title || 'unknown').toLowerCase() }; }
export function normalizeList(data, source) { const candidates = Array.isArray(data) ? data : data?.anime || data?.data || data?.results || data?.items || data?.list || []; return candidates.map(x => normalizeItem(x, source)).filter(x => x.title); }
